[project]
name = "firstresearch"
version = "0.1.0"
description = "First-principles auto-research agent prototype"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2",
  "PyYAML>=6",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

